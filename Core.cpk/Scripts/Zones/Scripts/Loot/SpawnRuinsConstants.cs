namespace AtomicTorch.CBND.CoreMod.Zones
{
    using System;

    public static class SpawnRuinsConstants
    {
        public static readonly TimeSpan SpawnInterval
            = TimeSpan.FromMinutes(30);
    }
}