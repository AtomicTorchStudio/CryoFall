<base:BaseUserControl
    x:Class="AtomicTorch.CBND.CoreMod.UI.Controls.Game.WorldObjects.Character.PlayerCharacterWeaponLineControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:base="clr-namespace:AtomicTorch.GameEngine.Common.Client.MonoGame.UI;assembly=AtomicTorch.CBND.GameApi.WPF.Stubs"
    xmlns:data="clr-namespace:AtomicTorch.CBND.CoreMod.UI.Controls.Game.WorldObjects.Character.Data"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="300"
    d:DataContext="{d:DesignInstance data:ViewModelPlayerCharacterWeaponLineControl, IsDesignTimeCreatable=True}"
    Visibility="{Binding Visibility}"
    IsHitTestVisible="False">

    <Canvas HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">
        <Line X1="{Binding ScreenCenterX}"
              Y1="{Binding ScreenCenterY}"
              X2="{Binding ScreenTargetX}"
              Y2="{Binding ScreenTargetY}"
              StrokeThickness="1"
              Stroke="#77FFFFFF"
              StrokeDashArray="4,3.75" />
    </Canvas>
</base:BaseUserControl>